# OOP ToDo List (CLI Task Manager)

Консольний менеджер завдань, написаний на Python із використанням об'єктно-орієнтованого підходу. Проєкт демонструє використання просунутих можливостей мови для створення чистого та розширюваного коду.

### 🛠 Технічні особливості (Python Core):
* **Decorators:** Реалізовано власний декоратор `@decorated_list` для стандартизації виводу даних у консоль.
* **Persistance Layer:** Збереження та відновлення стану об'єктів у `JSON` форматі з підтримкою кирилиці (`ensure_ascii=False`).
* **Error Handling:** Обробка винятків (`try/except/finally`) для безпечного завершення роботи програми та збереження даних навіть при `KeyboardInterrupt`.
* **Modern Syntax:** Використання конструкції `match/case` (Python 3.10+) для навігації в меню.

### 📋 Функціонал:
* **CRUD:** Створення, читання, оновлення (відмітка про виконання) та видалення завдань.
* **Auto-Management:** Автоматичний перерахунок ID завдань після видалення (`id_reload`) для збереження цілісності списку.
* **Data Serialization:** Конвертація об'єктів класу `Task` у словники (`__dict__`) для запису в файл.

### 🚀 Stack:
* **Python 3.10+**
* **Modules:** `json`
* **Concepts:** OOP, Decorators, Context Managers (`with open`).
